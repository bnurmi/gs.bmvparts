node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Large source archives that are NOT needed at build- or run-time.
# (Kept locally on this dev container only. Re-download from original sources
# if needed.)
data/etk/BMW-ETK-Minus-Large-Files.zip
data/etk/ETK-Data_*.jetarch
data/etk/ETK-Data_*.jetarch.part*
data/etk/rom-files/
data/psdzdata/
# Legacy v2 export — superseded by data/export-chunks/ (v3). 1.2 GB, unused.
data/export-data.json

# Production-required data that is fetched from Object Storage at build time
# (see script/build.ts -> syncExportFromObjectStorage). Source of truth:
#   export/export-manifest.json
#   export/chunks/chunk_NNN.json
data/export-manifest.json
data/export-chunks/

# Run-once local upload helper scripts (not part of the app)
upload_to_os.mjs
upload_one.mjs
upload_test.mjs

# Redis RDB snapshot — durable on the VM but not committed to the repo
data/redis/attached_assets/
data/psdzdata/
data/export-data.json
__pycache__/
.pytest_cache/
.venv/
.env
.env.*
*.db
*.sqlite
*.sqlite3
logs/
tmp/
.local/
.cache/
node_modules/
data/
attached_assets/
logs/
dist/
artifacts/
tokens/
.agents/
