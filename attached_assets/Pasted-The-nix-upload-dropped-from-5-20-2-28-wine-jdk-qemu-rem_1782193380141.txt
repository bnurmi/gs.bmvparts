The nix upload dropped from 5:20 → 2:28 — wine/jdk/qemu removal worked. But the referrer manifest is still blocking VM creation. This confirms it's not a size problem at all.

The referrer manifest is Replit's new SBOM/security attestation step (added between June 17 and now). The VM provisioner is either bugged handling it, or it's actively checking it. Let me see what's in the vulnerability report — if the provisioner is policy-gating on HIGH severity CVEs, fixing them removes the blocker:


3 actions
The timeline confirms it: nix upload dropped to 2:28 (huge win) but "Pushed referrer manifest" is still the last log line with no VM ever created. This is a Replit pipeline issue — the referrer manifest is new SBOM/security attestation infrastructure that's breaking the VM provisioner.

The vulnerability list is the clue. path-to-regexp, ws, vite, picomatch all have patched versions available. The referrer manifest's SBOM content includes these findings. Let me see what npm audit fix can resolve safely:


2 actions
The npm audit fix --dry-run timed out. Let me directly check the specific vulnerable packages and their fixable versions:


3 actions
All the vulnerable packages are already at their latest versions — vite@7.3.0, ws@8.18.0, path-to-regexp@8.3.0. There's no npm fix available. The audit flags are against the latest releases.

Let me lay out exactly what's happening and why, clearly:

What's actually broken
Replit added an SBOM security attestation step to their build pipeline after your June 17 deploy. It works like this:

Security scan runs and completes (~1:20 min)
Nix layer uploads (currently ~2:28 min — slower than the scan)
Image is assembled with the scan result attached as a "referrer manifest"
VM provisioner receives the image — then does nothing. No "Creating virtual machine" ever appears.
On June 17 there was no referrer manifest step. It's new infrastructure on Replit's end, and their VM provisioner appears to not handle it correctly.

The one remaining workaround that keeps VM deployment
The referrer manifest is only attached because the security scan finishes before the nix layer uploads. If the nix layer uploads faster than the scan (~1:20 min), the image is assembled before the scan result is ready → no referrer manifest → VM is provisioned.

That means emptying replit.nix almost entirely. The X11/Playwright/graphics packages (~30 packages) currently in there are what's making the nix layer slow. If we strip them, the nix closure becomes just the nodejs-20 module — probably a 20–30 second upload.

What you'd lose in dev: The scripts/verify-hub-seo.ts Playwright script needs those X11 system libraries to run Chromium. Everything else (the actual app, scraping, Redis, backfills) is unaffected.

What you'd keep: Redis still runs in production (it's in the run command), sessions, background workers, everything VM-specific.

Do you want me to strip the X11/Playwright packages from replit.nix? Or would you rather open a Replit support ticket about the VM provisioner bug (since that's the permanent fix)?