bind 127.0.0.1
loglevel warning

# RDB persistence — three save tiers
# snapshot if ≥1 key changed in the last hour
save 3600 1
# snapshot if ≥100 keys changed in the last 5 minutes
save 300 100
# snapshot if ≥10000 keys changed in the last minute
save 60 10000

dir /home/runner/workspace/data/redis
dbfilename dump.rdb
